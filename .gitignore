# Build output
/relay
/bin/
*.test

# Generated by `go test ./internal/contract/...` and consumed by
# `tools/contract/validate.mjs`. Regenerated on every run, so committing it
# would only ever be a stale copy of what the code currently produces.
/internal/contract/testdata/wire/

# Node tooling
node_modules/
# Built output of clients/typescript. The package publishes it; the repository
# does not carry it, for the same reason the wire fixtures are not carried.
/clients/typescript/dist/

# Coverage
*.out
coverage.txt
